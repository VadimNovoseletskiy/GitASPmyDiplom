@model NLayerApp.BusinessLogicLayer.Models.DeleteApartmentViewModel
@{
    ViewBag.Title = "Delete";
    Layout = "~/Views/Shared/_InputObjectForm.cshtml";
}

<h2>Видадення з бази даних</h2>
<h3>обєкту "Квартира"</h3>
<div>
    @if ((Model?.Id ?? 0) > 0)
    {
        <p class="axtyng">Увага!!!!</p>
        <p class="fz20"> Ви збираєтесь виладити з бази данних без змоги відновити об'єкт з такими параметрами:</p>
        <form method="POST" action="/AdminApartment/Delete">
            <table>
                <tr><td>іd :</td><td><input value="@Model.Id" name="Id" /></td></tr>
                <tr><td>населений пункт :</td><td>@Html.DropDownList("Village", ViewBag.villageName as SelectList, @Model.Village)</td></tr>
                <tr><td>район:</td><td>@Html.DropDownList("Region", ViewBag.regionName as SelectList, @Model.Region)</td></tr>
                <tr><td>вулиця:</td><td>@Html.DropDownList("Street", ViewBag.streetName as SelectList, @Model.Street)</td></tr>
                <tr><td>будинок №:</td><td><input value="@Model.NumberAdress" name="NumberAdress"/></td></tr>
                <tr><td>квартира №</td><td>@Model.ApartmentNumber</td></tr>
                <tr><td>тип операції:</td><td>@Model.OperationType</td></tr>
                <tr><td>тип кімнат:</td><td>@Model.TypeRoom</td></tr>
                <tr><td>кількість кімнат:</td><td>@Model.RoomsApartment</td></tr>
                <tr><td>детальна Інфо :</td><td>@Model.InfoDetails</td></tr>


            </table>
            <input type="submit" value="Видалити" />
        </form>
    }
</div>


<div class="returnToList">
    @Html.ActionLink("Повернутися до списку об'єктів 'Квартири' ", "Index")
</div>






